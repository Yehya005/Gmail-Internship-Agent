# Browser automation — controls a Chromium window to interact with Gmail
playwright==1.58.0

# RAG — semantic embeddings for CV–JD matching (cv_match.py).
# all-MiniLM-L6-v2 (~80 MB) runs on CPU, downloaded on first use.
sentence-transformers>=3.0
numpy>=1.26

# UI — dashboard reads history.jsonl produced by gmail_agent.py.
streamlit>=1.40

# ---------------------------------------------------------------
# IMPORTANT: Install using python -m pip (not bare pip) to ensure
# packages land in the same Python that runs the scripts:
#
#   python -m pip install -r requirements.txt
#   python -m playwright install chromium
#
# Playwright's Chromium binary is stored at:
#   ~\AppData\Local\ms-playwright\
# and is shared across Python versions — only needed once.
# ---------------------------------------------------------------
